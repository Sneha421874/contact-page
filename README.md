Objective
Create a contact form and validate user inputs (name, email, message) using client-side JavaScript.

Tools Used
VS Code

Chrome Browser

HTML, CSS, JavaScript

Features
Validates Name (cannot be empty)

Validates Email (must be in correct format)

Validates Message (minimum 10 characters)

Shows clear error messages

Shows a success message on valid submission

How to Run
Save the file as contact.html.

Place it in any folder (no restrictions).

Open it using:

Double-click in File Explorer or

Right-click → Open with Live Server in VS Code

Try entering invalid and valid inputs to test validation.

Deliverable
A working contact form with:

HTML form

JavaScript validation script

User feedback messages
